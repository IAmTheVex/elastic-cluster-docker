# Elastic Stack (elasticsearch + kibana) on Docker

To check if it's working run `curl http://localhost:9200/_nodes\?pretty\=true`

Kibana: http://localhost:8090/
Elasticsearch cluster: http://localhost:9200/
